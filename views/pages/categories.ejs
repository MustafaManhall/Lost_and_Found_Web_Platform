<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/header.ejs') %>
  <body>
    <%- include('../partials/nav.ejs') %>

    <style>
      .truncate {
        overflow: hidden;
        text-overflow: ellipsis;
        display: -webkit-box;
        -webkit-line-clamp: 3; /* Number of lines to show */
        line-clamp: 3; /* Standard property for compatibility */
        -webkit-box-orient: vertical;
      }
      .card-img-top {
        max-width: 100%; /* Set maximum width to 100% of its container */
        max-height: 300px; /* Set maximum height to 200 pixels */
        object-fit: cover; /* Ensure the image covers the container without stretching */
      }
    </style>

    <!-- Header -->
    <section class="mt-3 mb-2">
      <div class="container">
        <div class="text-center">
          <h1 style="color: #27374d">Lost & Found Categories</h1>
          <p style="color: #526d82">
            Discover and report lost or found belongings. Help reunite lost
            items with their owners through our community-driven platform.
          </p>
        </div>
      </div>
    </section>
    
    <!-- Electronics Category -->

    <section class="mt-3 mb-2">
      
      <div class="container">
        
        <div class="row">
          <h3 class="col text-start" style="color: #27374d">Electronics</h3>
          <div class="col text-end m-2" >
            <a
            class="link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
            style="font-weight: bold; color: #000;"
            href="/categories/Electronics"
            >See All</a>
          </div>
        </div>

        <div class="row">
          <% let count = 0; %>
          <% for (let i = 0; i < posts.length && count < 3; i++) { %>  
            <% if (posts[i].post_cate === 'Electronics') { %>
              <div class="col-sm mt-3">
                <div class="card h-100" style="border-radius: 24px">
                  <img
                    src="<%= posts[i].post_image %>"
                    class="card-img-top h-75 p-3"
                    style="border-radius: 24px"
                    alt=""
                  />
                  <div class="card-body">
                    <h5 class="card-title" style="color: #27374d">
                      <span class="<%= posts[i].post_lorf === 'Lost' ? 'text-warning' : posts[i].post_lorf === 'Found' ? 'text-success' : 'text-secondary' %>
                        "> <%= posts[i].post_lorf %> : </span> <%= posts[i].post_title %>
                    </h5>
                    <p class="card-text truncate" style="color: #526d82">
                      <%= posts[i].post_des %>
                    </p>
                    <a href="/post/<%= posts[i].post_id %>" class="btn btn-dark">Learn more</a>
                  </div>
                </div>
              </div>
              <% count++; %>
            <% } %>
          <% } %>
        </div>
        
      </div>
    </section>

    <!-- Jewelry & Accessorie Category -->

    <section class="mt-4 mb-2">
      <div class="container">
        <div class="row">
          <h3 class="col text-start" style="color: #27374d">
            Jewelry & Accessories
          </h3>
          <div class="col text-end m-2">
            <a
            class="link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
            style="font-weight: bold; color: #000;"
            href="/categories/Jewelry & Accessories"
            >See All</a>
          </div>
        </div>

        <div class="row">
          <% count = 0; %>
          <% for (let i = 0; i < posts.length && count < 3; i++) { %>  
            <% if (posts[i].post_cate === 'Jewelry & Accessories') { %>
              <div class="col-sm mt-3">
                <div class="card h-100" style="border-radius: 24px">
                  <img
                    src="<%= posts[i].post_image %>"
                    class="card-img-top h-75 p-3"
                    style="border-radius: 24px"
                    alt=""
                  />
                  <div class="card-body">
                    <h5 class="card-title" style="color: #27374d">
                      <span class="<%= posts[i].post_lorf === 'Lost' ? 'text-warning' : posts[i].post_lorf === 'Found' ? 'text-success' : 'text-secondary' %>
                        "> <%= posts[i].post_lorf %> : </span> <%= posts[i].post_title %>
                    </h5>
                    <p class="card-text truncate" style="color: #526d82">
                      <%= posts[i].post_des %>
                    </p>
                    <a href="/post/<%= posts[i].post_id %>" class="btn btn-dark">Learn more</a>
                  </div>
                </div>
              </div>
              <% count++; %>
            <% } %>
          <% } %>
        </div>

      </div>
    </section>

    <!-- Pets Category -->

    <section class="mt-4 mb-2">
      <div class="container">
        <div class="row">
          <h3 class="col text-start" style="color: #27374d">Pets</h3>
          <div class="col text-end m-2">
            <a
            class="link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
            style="font-weight: bold; color: #000;"
            href="/categories/Pets"
            >See All</a>
          </div>
        </div>

        <div class="row">
          <% count = 0; %>
          <% for (let i = 0; i < posts.length && count < 3; i++) { %>  
            <% if (posts[i].post_cate === 'Pets') { %>
              <div class="col-sm mt-3">
                <div class="card h-100" style="border-radius: 24px">
                  <img
                    src="<%= posts[i].post_image %>"
                    class="card-img-top h-75 p-3"
                    style="border-radius: 24px"
                    alt=""
                  />
                  <div class="card-body">
                    <h5 class="card-title" style="color: #27374d">
                      <span class="<%= posts[i].post_lorf === 'Lost' ? 'text-warning' : posts[i].post_lorf === 'Found' ? 'text-success' : 'text-secondary' %>
                        "> <%= posts[i].post_lorf %> : </span> <%= posts[i].post_title %>
                    </h5>
                    <p class="card-text truncate" style="color: #526d82">
                      <%= posts[i].post_des %>
                    </p>
                    <a href="/post/<%= posts[i].post_id %>" class="btn btn-dark">Learn more</a>
                  </div>
                </div>
              </div>
              <% count++; %>
            <% } %>
          <% } %>
        </div>

      </div>
    </section>

    <!-- Apparel & Accessories Category -->

    <section class="mt-4 mb-2">
      <div class="container">
        <div class="row">
          <h3 class="col text-start" style="color: #27374d">
            Apparel & Accessories
          </h3>
          <div class="col text-end m-2">
            <a
          class="link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
          style="font-weight: bold; color: #000;"
            href="/categories/Apparel & Accessories"
            >See All</a>
          </div>
        </div>

        <div class="row">
          <% count = 0; %>
          <% for (let i = 0; i < posts.length && count < 3; i++) { %>  
            <% if (posts[i].post_cate === 'Apparel & Accessories') { %>
              <div class="col-sm mt-3">
                <div class="card h-100" style="border-radius: 24px">
                  <img
                    src="<%= posts[i].post_image %>"
                    class="card-img-top h-75 p-3"
                    style="border-radius: 24px"
                    alt=""
                  />
                  <div class="card-body">
                    <h5 class="card-title" style="color: #27374d">
                      <span class="<%= posts[i].post_lorf === 'Lost' ? 'text-warning' : posts[i].post_lorf === 'Found' ? 'text-success' : 'text-secondary' %>
                        "> <%= posts[i].post_lorf %> : </span> <%= posts[i].post_title %>
                    </h5>
                    <p class="card-text truncate" style="color: #526d82">
                      <%= posts[i].post_des %>
                    </p>
                    <a href="/post/<%= posts[i].post_id %>" class="btn btn-dark">Learn more</a>
                  </div>
                </div>
              </div>
              <% count++; %>
            <% } %>
          <% } %>
        </div>

      </div>
    </section>

    <!-- Documents and IDs Category -->

    <section class="mt-4 mb-2">
      <div class="container">
        <div class="row">
          <h3 class="col text-start" style="color: #27374d">
            Documents and IDs
          </h3>
          <div class="col text-end m-2">
            <a
          class="link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
          style="font-weight: bold; color: #000;"
            href="/categories/Documents and IDs"
            >See All</a>
          </div>
        </div>

        <div class="row">
          <% count = 0; %>
          <% for (let i = 0; i < posts.length && count < 3; i++) { %>  
            <% if (posts[i].post_cate === 'Documents and IDs') { %>
              <div class="col-sm mt-3">
                <div class="card h-100" style="border-radius: 24px">
                  <img
                    src="<%= posts[i].post_image %>"
                    class="card-img-top h-75 p-3"
                    style="border-radius: 24px"
                    alt=""
                  />
                  <div class="card-body">
                    <h5 class="card-title" style="color: #27374d">
                      <span class="<%= posts[i].post_lorf === 'Lost' ? 'text-warning' : posts[i].post_lorf === 'Found' ? 'text-success' : 'text-secondary' %>
                        "> <%= posts[i].post_lorf %> : </span> <%= posts[i].post_title %>
                    </h5>
                    <p class="card-text truncate" style="color: #526d82">
                      <%= posts[i].post_des %>
                    </p>
                    <a href="/post/<%= posts[i].post_id %>" class="btn btn-dark">Learn more</a>
                  </div>
                </div>
              </div>
              <% count++; %>
            <% } %>
          <% } %>
        </div>

      </div>
    </section>

    <!-- Others Category -->

    <section class="mt-4 mb-2">
      <div class="container">
        <div class="row">
          <h3 class="col text-start" style="color: #27374d">Others</h3>
          <div class="col text-end m-2">
            <a
            class="link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover"
            style="font-weight: bold; color: #000;"
            href="/categories/Others"
            >See All</a>
          </div>
        </div>

        <div class="row">
          <% count = 0; %>
          <% for (let i = 0; i < posts.length && count < 3; i++) { %>  
            <% if (posts[i].post_cate === 'Others') { %>
              <div class="col-sm mt-3">
                <div class="card h-100" style="border-radius: 24px">
                  <img
                    src="<%= posts[i].post_image %>"
                    class="card-img-top h-75 p-3"
                    style="border-radius: 24px"
                    alt=""
                  />
                  <div class="card-body">
                    <h5 class="card-title" style="color: #27374d">
                      <span class="<%= posts[i].post_lorf === 'Lost' ? 'text-warning' : posts[i].post_lorf === 'Found' ? 'text-success' : 'text-secondary' %>
                        "> <%= posts[i].post_lorf %> : </span> <%= posts[i].post_title %>
                    </h5>
                    <p class="card-text truncate" style="color: #526d82">
                      <%= posts[i].post_des %>
                    </p>
                    <a href="/post/<%= posts[i].post_id %>" class="btn btn-dark">Learn more</a>
                  </div>
                </div>
              </div>
              <% count++; %>
            <% } %>
          <% } %>
        </div>

      </div>
    </section>
   
    <%- include('../partials/footer.ejs')%>
  </body>
</html>


<!-- 






-->